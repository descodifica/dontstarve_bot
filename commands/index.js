const Canais = require('./Canais')
const Config = require('./Config')

module.exports = {
  ptbr: {
    canais: Canais,
    config: Config,
  },
  en: {
    channels: Canais,
    config: Config,
  },
}